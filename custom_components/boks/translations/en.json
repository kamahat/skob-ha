{
  "config": {
    "flow_title": "{name}",
    "step": {
      "confirm": {
        "description": "Add {name}?"
      },
      "user": {
        "description": "Select the Boks to add.",
        "data": {
          "address": "Device"
        }
      }
    },
    "abort": {
      "no_devices_found": "No Boks found. Make sure a Bluetooth adapter or an ESPHome Bluetooth proxy is in range, and that the official dongle is unplugged (it holds the connection).",
      "already_configured": "This Boks is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Link settings",
        "description": "Saving reloads the link without restarting Home Assistant. Note: the integration's Python code is not reloaded — after updating the component files, a restart is still required.",
        "data": {
          "label": "Mailbox identifier (optional)",
          "keepalive": "Keepalive interval",
          "reconnect_max": "Maximum delay between reconnection attempts",
          "open_code": "Open code (optional)"
        },
        "data_description": {
          "label": "For example F540 — the reference printed on your mailbox or shown in your account. The Boks does not expose it over Bluetooth (its Serial Number characteristic returns its own MAC address), so it has to be typed in. The device will then be named “Boks F540”: essential as soon as you manage several mailboxes, which would otherwise all carry the same name.",
          "keepalive": "The Boks closes the connection after about 30 s of silence. A longer interval means fewer exchanges (and lower power draw) while the link is held, but moves closer to that cutoff: beyond it the link drops and reconnects in a loop, which costs more.",
          "reconnect_max": "Ceiling for the delay, which doubles on every failure. A high value spares the batteries while the Boks is out of range; a low one shortens the time to come back online.",
          "open_code": "Either the code itself (6 characters over the alphabet 0123456789AB), or a reference to secrets.yaml written as: !secret boks_code1 — recommended, so the code stays in the file meant for secrets. Filling this in adds an Open button: anyone with access to Home Assistant will then be able to open the mailbox. Leaving it empty keeps the integration strictly read-only. Use a permanent code (master or fixed) — a single-use code would work exactly once."
        }
      }
    },
    "error": {
      "invalid_open_code": "Invalid code: 6 characters, only 0-9, A and B.",
      "unknown_secret": "That key was not found in secrets.yaml (or holds no text)."
    }
  }
}
